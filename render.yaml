# Nothing left for Render's Blueprint to manage — the frontend now lives on Cloudflare Pages
# (spare-parts-management.pages.dev) and the backend runs as spareparts-backend-sg in Singapore,
# created directly via the Render dashboard rather than through this Blueprint (see
# commit ce91815, "Remove the old Oregon backend and expired Postgres from the Blueprint").
# The old spareparts-frontend static site (pointing at the now-deleted Oregon backend) is being
# retired too — keeping its entry here would let the next Blueprint sync recreate it after it's
# deleted from the Render dashboard.
services: []
